export function Footer() {
  return (
    <footer className="border-t border-border">
      <div className="max-w-[1180px] mx-auto px-5 sm:px-8 py-8 flex flex-col sm:flex-row items-center justify-between gap-3 text-sm text-muted">
        <p className="font-display italic text-ink-secondary">Velora</p>
        <p>A considered place to host and book live sessions.</p>
      </div>
    </footer>
  );
}
